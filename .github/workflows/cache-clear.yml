name: "Cleanup caches by a branch"

on: # yamllint disable-line rule:truthy
    pull_request:
        types:
            - "closed"

permissions:
    contents: "read"

jobs:
    cleanup-branch-cache:
        uses: "anolilab/workflows/.github/workflows/cleanup-branch-cache.yaml@6d0f01d0742f15c51b2398bc97e0b008c7416b00" # main
        with:
            target-repo: "anolilab/semantic-release"
